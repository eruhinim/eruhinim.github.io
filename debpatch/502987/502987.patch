=== modified file 'worm/worm.c'
--- worm/worm.c	2008-10-21 15:15:04 +0000
+++ worm/worm.c	2008-10-21 15:15:06 +0000
@@ -129,7 +129,7 @@
 		start_len = LENGTH;
 	stw = newwin(1, COLS-1, 0, 0);
 	tv = newwin(LINES-1, COLS-1, 1, 0);
-	box(tv, '*', '*');
+	box(tv, '|', '-');
 	scrollok(tv, FALSE);
 	scrollok(stw, FALSE);
 	wmove(stw, 0, 0);

